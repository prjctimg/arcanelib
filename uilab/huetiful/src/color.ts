/**
 * An object containing target steps/rotations on the color wheel. Generates different color schemes.
  */
const hueSteps = {[]};
/**
 * An object which holds the computed palettes.
 */
const palettes = {};


/**
 * Will contain preset color ranges in HEX for use in interolations and 
 */
type moodHues {
/* Paste in named.js in culori */

}